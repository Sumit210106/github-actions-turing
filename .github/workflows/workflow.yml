name: print Hello World 
on:
    push:
jobs:
    print:
        runs-on: ubuntu-latest
        steps:
          - name: print Hello
            run: echo "Hello World" 
          - name: Run script
            run: node backend/runWorkflow.js
